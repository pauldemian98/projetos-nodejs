# .env.example

# Configurações do Servidor
NODE_ENV=development
PORT=3000

# Configurações do Banco de Dados (Prisma)
# Modifique o usuário, senha, host, porta e nome do banco de acordo com sua configuração do MySQL
DATABASE_URL="mysql://root:Samsung1938%23@localhost:3306/piatec_helpdesk"
SMTP_HOST="smtp.office365.com"
SMTP_PORT=587
SMTP_USER="protheus.naoresponda@piatec.com"
SMTP_PASS="#Piatec@2026"
JWT_SECRET="piatec_academy_secret_token_change_me"

# Autenticação (JWT)
# Informe um segredo seguro para a geração dos tokens
JWT_SECRET="seu_segredo_super_seguro_aqui"
JWT_EXPIRES_IN="1d"

# Outras variáveis de ambiente (Ex: AWS S3 para anexos, SendGrid para e-mails, etc)
# STORAGE_BUCKET="nome_do_bucket"
# SMTP_HOST="smtp.example.com"

# API Externa (M�dulo Financeiro PBI)
EXTERNAL_FINANCIAL_API="https://piacentinido114560.protheus.cloudtotvs.com.br:4050/rest"

